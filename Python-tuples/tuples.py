"""
A tuple is actually very similar to a list. 
It's basically a structure where we can store multiple pieces of information. 
But a tuple has a few key differences from lists.
A tuple is immutable. Once created, it cannot be modified.
"""

coordinates = [(4, 5), (5, 6), (6, 7)] ##list with tuples inside
print(coordinates)